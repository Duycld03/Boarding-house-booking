{
  "name": "boarding house booking",
  "version": "1.0.0",
  "description": "project boarding house booking of group 4",
  "main": "index.js",
  "scripts": {
    "client": "cd frontend && npm start",
    "server": "cd backend && npm start",
    "dashboard": "cd dashboard && npm run dev",
    "start": "concurrently \"npm run client\" \"npm run server\" \"npm run dashboard\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.0.1"
  }
}
